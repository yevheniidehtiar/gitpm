{
  "name": "gitpm-monorepo",
  "private": true,
  "license": "MIT",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "vitest run",
    "lint": "biome check .",
    "dev:ui": "bun run --filter @gitpm/ui dev"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.10",
    "@types/node": "^25.5.2",
    "tsup": "^8.3.0",
    "typescript": "^6.0.2",
    "vitest": "^4.1.2"
  }
}
